/* setTimeout()
    setTimeout(()=>{
        console.log("This will be logged out after 1 second.");
    }, 1000);
*/

//fetching the user
function userDetails(userID, callback){
    setTimeout(()=>{
        if (userID === admin){
            const user ={ name: "ADMIN"};
            
        }
    }, 1000);
}