function getArea(l, w){
    area = length * width;
    return a;
}

const length = 5;
const width = 3;
const area = getArea(l, w);
consonle.log('The area of the rectangle with length ${length} and width ${width} is: ${area}')